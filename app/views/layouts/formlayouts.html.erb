<!DOCTYPE html>
<html>
  <head>
    <title>Ghostchat</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <% flash.each do |name, msg| %>
      <%= content_tag :div, msg, class: "alert alert-info" %>
    <% end %>
    <div class="row group-new-section-1">
    	<div class="col-md-6">
    		<div class="group-header">
    			<img src="<%= image_url 'ghost-md.png' %>" width="150">
    			<h1>Encrypted,<br> Expiring Group Chat</h1>
<!--     			<ul class="home-description"> -->
            <p class="home-bullets">Create a group in seconds.<br> Invite friends/team members.<br>
            Share, chat, work together.<br> Once your group expires, it's gone!
            </p>
<!--           </ul> -->
    		</div>
    	</div>

    	<div class="col-md-6">
    		<!-- Padding Inside Div -->
    		<div class="group-new-right-top">
          <%= yield %>
        </div>
      </div>
    </div>
</body>
</html>
